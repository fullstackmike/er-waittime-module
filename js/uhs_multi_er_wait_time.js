(function ($) {
  Drupal.behaviors.exampleModule = {
    attach: function (context, settings) {
      //look for waittime_list_display variable
      //set active/inactive on map
      console.log("find wait time listing display default : "+multi_er_list_display);
    }
  };
}(jQuery));